using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayfabStart : PlayfabLoginRegister
{
    // Start is called before the first frame update
    void Start()
    {
        LoginUser();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
